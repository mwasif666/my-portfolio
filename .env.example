# Copy to `.env` if you need to override the delivery cloud. `.env` is
# gitignored. The cloud name is safe in the browser bundle because it already
# appears in every Cloudinary delivery URL.

VITE_CLOUDINARY_CLOUD_NAME=agymx2xx
